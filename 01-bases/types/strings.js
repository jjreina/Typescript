"use strict";
// Anonymous self-invoking function
(() => {
    var _a;
    const batman = "Batman";
    const superman = "Super Man";
    const spiderman = `Hero: Spoderman`;
    console.log(batman.toUpperCase());
    console.log(`I´m ${batman}`);
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || "Not value");
})();
